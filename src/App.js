import React from 'react';
import Map from './Map'; // Import the Map component

const App = () => {
  return (
    <div>
      <Map /> {/* Render the Map component */}
    </div>
  );
};

export default App;
